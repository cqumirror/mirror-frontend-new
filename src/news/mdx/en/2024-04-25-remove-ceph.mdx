export const meta = {
    title: "Announcement on Removing ceph Repository",
    date: "2024-04-25",
    author: "cyy",
    summary: "The ceph repository has low traffic, so storage will be removed and redirected via 302 to USTC mirror.",
    tags: ["Announcement", "Removal"],
}

Statistics show that the ceph repository has low traffic, with almost no access other than crawlers. Due to limited disk array redundancy, we have decided to temporarily remove the ceph repository from local storage and redirect via 302 to the USTC mirror.

Affected repositories:

- /ceph/

Current status:

- Public access removed
- Storage removed

Resolution:

- 302 redirect
