## Kali Linux Mirror Usage Guide

## URL
https://mirrors.cqu.edu.cn/kali

## Description
The official package repository for Kali Linux.

## Supported Architectures

- x86_64
- x86

## Usage
### Detailed Step-by-Step Guide

These steps assume you are a beginner Linux user. Please follow along step by step.

#### 1. Locate and Back Up `sources.list`

```bash
$ cd /etc/apt/
$ sudo cp sources.list sources.list.bak
```

#### 2. Edit the `sources.list` File
A typical unmodified `sources.list` file looks like this:

```bash
$ sudo vim sources.list

---------------------
# sources.list
---------------------

# deb cdrom:[Debian GNU/Linux 2019.3 _Kali-rolling_ - Official Snapshot amd64 LIVE/INSTALL Binary 20190827-10:52]/ kali-last-snapshot contrib main non-free

# deb cdrom:[Debian GNU/Linux 2019.3 _Kali-rolling_ - Official Snapshot amd64 LIVE/INSTALL Binary 20190827-10:52]/ kali-last-snapshot contrib main non-free

deb http://http.kali.org/kali kali-rolling main non-free contrib
# deb-src http://http.kali.org/kali kali-rolling main non-free contrib

# This system was installed using small removable media
# (e.g. netinst, live or single CD). The matching "deb cdrom"
# entries were disabled at the end of the installation process.
# For information about how to configure apt package sources,
# see the sources.list(5) manual.
```

#### 3. Add the Mirror Source

> The following uses `vim` as an example.

1. Press `i` to enter edit mode.
2. Add the content shown below, or replace `http.kali.org` with `mirrors.cqu.edu.cn`.

```txt
deb https://mirrors.cqu.edu.cn/kali kali-rolling main non-free contrib
# deb-src https://mirrors.cqu.edu.cn/kali kali-rolling main non-free contrib
```

3. Press `Esc` to exit edit mode, then press `shift`+`;`, type `w`+`q`, and press Enter to save.

#### 4. Refresh the Cache

```bash
$ sudo apt-get update
```

### Quick Replacement

Run the following command directly in the terminal to make the change:
```bash
sudo sed -i s/http.kali.org/mirrors.cqu.edu.cn/ /etc/apt/sources.list
```


For more details about the `sed` command, please refer to the `sed` man page or the [Arch Linux Wiki](https://wiki.archlinux.org/index.php/Core_utilities#Essentials).


## Related Links
Kali Linux Homepage
https://www.kali.org/

Documentation
https://www.kali.org/kali-linux-documentation/

Forum
http://forums.kali.org/
